"""
SQLAlchemy models — for reference and potential ORM usage later.
Currently the app uses raw SQL via db/queries.py.
"""
# Intentionally minimal for MVP.
# The schema is defined in migrations/001_initial.sql.
# Add ORM models here when/if migrating from raw SQL.